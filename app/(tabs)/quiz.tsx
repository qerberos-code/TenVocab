import { useRouter } from 'expo-router';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { useEffect } from 'react';
import { Card, Screen, colors, s } from '@/ui';
import { useStore } from '@/store';
import { VOCAB } from '@/vocab';
export default function Quiz(){ const r=useRouter(); const st=useStore(); useEffect(()=>{ if(!st.todayIds.length) st.startSession(); },[]); const ids=st.todayIds.length?st.todayIds:VOCAB.slice(0,10).map(w=>w.id); const idx=Math.min(st.sessionIndex, ids.length-1); const w=st.getWord(ids[idx]); const choose=(choice:string)=>{const correct=choice===w.answer; st.recordQuiz(w.id,correct); r.push({pathname:'/results',params:{word:w.word,correct:correct?'1':'0',explanation:w.explanation}});}; return <Screen><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={s.scroll}><View style={s.row}><Text style={{fontSize:14,fontWeight:'700',color:colors.muted}}>Words in Context</Text><Text style={{fontSize:14,fontWeight:'800'}}>{idx+1}/{ids.length}</Text></View><Text style={[s.title,{marginTop:18}]}>Choose the best fit.</Text><Card style={{marginTop:18}}><Text style={{fontSize:20,lineHeight:30,fontWeight:'600'}}>{w.question.replace('_____','_____')}</Text></Card><View style={{gap:10,marginTop:16}}>{w.choices.map(c=><Pressable key={c} onPress={()=>choose(c)} style={({pressed})=>[{padding:17,borderRadius:16,borderWidth:1,borderColor:colors.line,backgroundColor:'#fff'},pressed&&{opacity:.72}]}><Text style={{fontSize:17,fontWeight:'700'}}>{c}</Text></Pressable>)}</View><Text style={[s.subtitle,{marginTop:14}]}>This prototype uses original practice questions modeled on vocabulary-in-context practice; it does not copy official SAT questions.</Text></ScrollView></Screen> }
